export function Observatory() {
  return (
    <section className="page">
      <h1>Observatory</h1>
      <p className="lede">
        Agents, timeline, disagreements, evidence, and transcript will render
        from existing events inside a debate.
      </p>
    </section>
  );
}
