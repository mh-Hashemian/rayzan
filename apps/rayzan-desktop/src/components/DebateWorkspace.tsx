export function DebateWorkspace() {
  return (
    <section className="page">
      <h1>Debate workspace</h1>
      <p className="lede">
        Stages, agent positions, and synthesis land in a later 3C.2
        checkpoint. Observatory stays inside a debate, not in global
        navigation.
      </p>
    </section>
  );
}
